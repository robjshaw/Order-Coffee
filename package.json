{
  "name": "df-init",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "airtable": "^0.11.0",
    "analytics-node": "^4.0.1",
    "faker": "^5.5.3",
    "segment": "^0.1.3"
  }
}
